<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AegisTune.App.Pages.HealthPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="using:AegisTune.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="{StaticResource PagePadding}" Spacing="20">
            <Border Style="{StaticResource HeroSurfaceCardStyle}">
                <Grid ColumnSpacing="18" RowSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="320" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="12">
                        <TextBlock Style="{StaticResource PageEyebrowTextStyle}" Text="WINDOWS HEALTH" />
                        <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Windows Health" />
                        <TextBlock Style="{StaticResource PageLeadTextStyle}" Text="{x:Bind HeroSummaryLine}" TextWrapping="WrapWholeWords" />
                        <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind ModuleStatusLine}" TextWrapping="WrapWholeWords" />
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button
                                Click="RefreshHealth_Click"
                                Content="Review Windows health"
                                Style="{StaticResource PrimaryCtaButtonStyle}" />
                            <Button
                                Click="OpenWindowsUpdate_Click"
                                Content="Open Windows Update"
                                Style="{StaticResource SecondaryCtaButtonStyle}" />
                        </StackPanel>
                    </StackPanel>

                    <Border Grid.Column="1" Style="{StaticResource CompactSurfaceCardStyle}">
                        <StackPanel Spacing="8">
                            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="What happens next" />
                            <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="Review crash and update evidence first. Then open Services or Task Scheduler only for the items that are actually flagged." TextWrapping="WrapWholeWords" />
                            <TextBlock Style="{StaticResource CaptionTextStyle}" Text="{x:Bind ActionStatusLine}" TextWrapping="WrapWholeWords" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Grid ColumnSpacing="16" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource SurfaceCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource MetricCaptionTextStyle}" Text="Items to review" />
                        <TextBlock Style="{StaticResource MetricValueTextStyle}" Text="{x:Bind IssueCountLabel}" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource SurfaceCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource MetricCaptionTextStyle}" Text="Crash evidence" />
                        <TextBlock Style="{StaticResource MetricValueTextStyle}" Text="{x:Bind CrashCountLabel}" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource SurfaceCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource MetricCaptionTextStyle}" Text="Windows Update issues" />
                        <TextBlock Style="{StaticResource MetricValueTextStyle}" Text="{x:Bind WindowsUpdateIssueCountLabel}" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource SurfaceCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource MetricCaptionTextStyle}" Text="Background review" />
                        <TextBlock Style="{StaticResource MetricValueTextStyle}" Text="{x:Bind ServiceReviewCountLabel}" />
                        <TextBlock Style="{StaticResource CaptionTextStyle}" Text="{x:Bind ScheduledTaskReviewCountLabel}" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource CompactSurfaceCardStyle}">
                <CommandBar Style="{StaticResource ActionCommandBarStyle}">
                    <CommandBar.PrimaryCommands>
                        <AppBarButton Click="OpenServices_Click" Label="Open Services" />
                        <AppBarButton Click="OpenTaskScheduler_Click" Label="Open Task Scheduler" />
                    </CommandBar.PrimaryCommands>
                    <CommandBar.SecondaryCommands>
                        <AppBarButton Click="OpenEventViewer_Click" Label="Open Event Viewer" />
                    </CommandBar.SecondaryCommands>
                </CommandBar>
            </Border>

            <Border Style="{StaticResource SurfaceCardStyle}">
                <Grid ColumnSpacing="18" RowSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="320" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="8">
                        <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Selected issue right now" />
                        <TextBlock Style="{StaticResource PageLeadTextStyle}" Text="{x:Bind SelectedHealthFocusTitle}" TextWrapping="WrapWholeWords" />
                        <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind SelectedHealthFocusSummary}" TextWrapping="WrapWholeWords" />
                        <TextBlock Style="{StaticResource CaptionTextStyle}" Text="{x:Bind SelectedHealthFocusNextStep}" TextWrapping="WrapWholeWords" />
                    </StackPanel>

                    <Border Grid.Column="1" Style="{StaticResource InsetSurfaceCardStyle}">
                        <StackPanel Spacing="10">
                            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Next button to press" />
                            <Button
                                Click="RunSelectedHealthPrimaryAction_Click"
                                Content="{x:Bind SelectedHealthPrimaryActionLabel}"
                                Style="{StaticResource PrimaryCtaButtonStyle}"
                                Visibility="{x:Bind SelectedHealthPrimaryActionVisibility}" />
                            <Button
                                Click="RunSelectedHealthSecondaryAction_Click"
                                Content="{x:Bind SelectedHealthSecondaryActionLabel}"
                                Style="{StaticResource SecondaryCtaButtonStyle}"
                                Visibility="{x:Bind SelectedHealthSecondaryActionVisibility}" />
                            <TextBlock Style="{StaticResource CaptionTextStyle}" Text="Use the review buttons in the lists below to change the selected issue without losing context." TextWrapping="WrapWholeWords" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Recent crash evidence" />
            <Border Style="{StaticResource CompactSurfaceCardStyle}">
                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind CrashSummary}" TextWrapping="WrapWholeWords" />
            </Border>
            <ItemsControl ItemsSource="{x:Bind CrashEvents}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="core:WindowsHealthEventRecord">
                        <Border Margin="0,0,0,12" Style="{StaticResource SurfaceCardStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" />
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="{x:Bind EventLabel}" />
                                    <TextBlock Text="•" />
                                    <TextBlock Text="{x:Bind Severity}" />
                                    <TextBlock Text="•" />
                                    <TextBlock Text="{x:Bind OccurredAtLabel}" />
                                </StackPanel>
                                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind Detail}" TextWrapping="WrapWholeWords" />
                                <CommandBar Style="{StaticResource ActionCommandBarStyle}">
                                    <CommandBar.PrimaryCommands>
                                        <AppBarButton Click="FocusCrashEvent_Click" Label="Review this crash" Tag="{Binding}" />
                                    </CommandBar.PrimaryCommands>
                                </CommandBar>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Windows Update issues" />
            <Border Style="{StaticResource CompactSurfaceCardStyle}">
                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind WindowsUpdateSummary}" TextWrapping="WrapWholeWords" />
            </Border>
            <ItemsControl ItemsSource="{x:Bind WindowsUpdateEvents}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="core:WindowsHealthEventRecord">
                        <Border Margin="0,0,0,12" Style="{StaticResource SurfaceCardStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" />
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="{x:Bind EventLabel}" />
                                    <TextBlock Text="•" />
                                    <TextBlock Text="{x:Bind Severity}" />
                                    <TextBlock Text="•" />
                                    <TextBlock Text="{x:Bind OccurredAtLabel}" />
                                </StackPanel>
                                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind Detail}" TextWrapping="WrapWholeWords" />
                                <CommandBar Style="{StaticResource ActionCommandBarStyle}">
                                    <CommandBar.PrimaryCommands>
                                        <AppBarButton Click="FocusWindowsUpdateEvent_Click" Label="Review this issue" Tag="{Binding}" />
                                    </CommandBar.PrimaryCommands>
                                </CommandBar>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Services to review" />
            <Border Style="{StaticResource CompactSurfaceCardStyle}">
                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind ServiceSummary}" TextWrapping="WrapWholeWords" />
            </Border>
            <ItemsControl ItemsSource="{x:Bind ServiceCandidates}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="core:ServiceReviewRecord">
                        <Border Margin="0,0,0,12" Style="{StaticResource SurfaceCardStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind DisplayTitle}" />
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="{x:Bind StartModeLabel}" />
                                    <TextBlock Text="•" />
                                    <TextBlock Text="{x:Bind StateLabel}" />
                                </StackPanel>
                                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind Issue}" TextWrapping="WrapWholeWords" />
                                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind ExecutablePathLabel}" TextWrapping="WrapWholeWords" />
                                <CommandBar Style="{StaticResource ActionCommandBarStyle}">
                                    <CommandBar.PrimaryCommands>
                                        <AppBarButton Click="FocusServiceReview_Click" Label="Review this service" Tag="{Binding}" />
                                        <AppBarButton Click="OpenServiceExecutable_Click" IsEnabled="{x:Bind ExecutablePathExists}" Label="Open service target" Tag="{Binding}" />
                                    </CommandBar.PrimaryCommands>
                                </CommandBar>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Scheduled tasks to review" />
            <Border Style="{StaticResource CompactSurfaceCardStyle}">
                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind ScheduledTaskSummary}" TextWrapping="WrapWholeWords" />
            </Border>
            <ItemsControl ItemsSource="{x:Bind ScheduledTaskCandidates}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="core:ScheduledTaskReviewRecord">
                        <Border Margin="0,0,0,12" Style="{StaticResource SurfaceCardStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind TaskName}" />
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="{x:Bind DisplayPath}" />
                                    <TextBlock Text="•" />
                                    <TextBlock Text="{x:Bind StateLabel}" />
                                </StackPanel>
                                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind Issue}" TextWrapping="WrapWholeWords" />
                                <TextBlock Style="{StaticResource MutedBodyTextStyle}" Text="{x:Bind ExecutePathLabel}" TextWrapping="WrapWholeWords" />
                                <CommandBar Style="{StaticResource ActionCommandBarStyle}">
                                    <CommandBar.PrimaryCommands>
                                        <AppBarButton Click="FocusScheduledTaskReview_Click" Label="Review this task" Tag="{Binding}" />
                                        <AppBarButton Click="OpenScheduledTaskExecutable_Click" IsEnabled="{x:Bind ExecutePathExists}" Label="Open task target" Tag="{Binding}" />
                                    </CommandBar.PrimaryCommands>
                                </CommandBar>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>
